# smithy

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build smithy` to build the library.

## Running unit tests

Run `nx test smithy` to execute the unit tests via [Vitest](https://vitest.dev/).
