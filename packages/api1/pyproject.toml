[tool.coverage.run]
branch = true
source = [ "aws_nx_poc_api1" ]

[tool.coverage.report]
exclude_lines = [ "if TYPE_CHECKING:" ]
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../coverage/packages/api1/html'
 --cov-report xml:'../../coverage/packages/api1/coverage.xml'
 --html='../../reports/packages/api1/unittests/html/index.html'
 --junitxml='../../reports/packages/api1/unittests/junit.xml'
"""

[tool.hatch.build.targets.wheel]
packages = [ "aws_nx_poc_api1" ]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
exclude = [ ".ruff_cache", ".svn", ".tox", ".venv", "dist" ]
line-length = 120
indent-width = 4

  [tool.ruff.lint]
  select = [ "E", "F", "UP", "B", "SIM", "I" ]
  ignore = [ ]
  fixable = [ "ALL" ]
  unfixable = [ ]

[tool.uv.sources.aws-nx-poc-postgres-db]
workspace = true

[project]
name = "aws-nx-poc-api1"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.14"
readme = "README.md"
dependencies = [
  "fastapi==0.138.2",
  "uvicorn==0.49.0",
  "aws-lambda-powertools==3.31.0",
  "aws-lambda-powertools[tracer]==3.31.0",
  "aws-lambda-powertools[parser]==3.31.0",
  "aws-nx-poc-postgres-db"
]

[build-system]
requires = [ "hatchling" ]
build-backend = "hatchling.build"

[dependency-groups]
dev = [ "fastapi[standard]==0.138.2" ]
