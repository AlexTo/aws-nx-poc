# postgres-db

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build postgres-db` to build the library.

## Running unit tests

Run `nx test postgres-db` to execute the unit tests via [Vitest](https://vitest.dev/).
