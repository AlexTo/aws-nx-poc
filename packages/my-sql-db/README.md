# my-sql-db

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build my-sql-db` to build the library.

## Running unit tests

Run `nx test my-sql-db` to execute the unit tests via [Vitest](https://vitest.dev/).
