# dynamodb1

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dynamodb1` to build the library.

## Running unit tests

Run `nx test dynamodb1` to execute the unit tests via [Vitest](https://vitest.dev/).
