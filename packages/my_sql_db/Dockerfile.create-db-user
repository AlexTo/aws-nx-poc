FROM public.ecr.aws/lambda/python:3.14

WORKDIR ${LAMBDA_TASK_ROOT}

COPY . .

RUN curl -fsSL "https://truststore.pki.rds.amazonaws.com/global/global-bundle.pem" \
    -o ./global-bundle.pem

CMD ["aws_nx_poc_my_sql_db.create_db_user_handler.handler"]
